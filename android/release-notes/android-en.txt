A new local Bedrock WebSocket bridge runs on 127.0.0.1:19134. Interactions now execute in the background, so Minecraft chat does not open during events. The app shows live connection status, copies the /wsserver connection command, and only permits tests when Minecraft is truly connected. The former accessibility helper, CraftLive keyboard, and screen overlay are no longer required. The fixed five-second safety delay remains active.
