CraftLive is fixed to portrait mode again. The Edit button in the top Calibrate chat button menu opens Minecraft automatically and displays a draggable target that can be placed on the chat icon. Saving stores the exact position and immediately removes the target and controls. Tests then use only this manually calibrated point. The app preserves and verifies permissions on every launch and only shows a step that is actually missing.
