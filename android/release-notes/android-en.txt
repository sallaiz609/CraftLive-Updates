After opening Bedrock chat, CraftLive now explicitly focuses the real command input field. It tries multiple compatible Android input methods and sends Enter only after verified text entry. If input fails, chat closes automatically instead of remaining open during gameplay. A local Bedrock world cannot be controlled fully in the background by an external Android app, so chat only flashes briefly when an interaction succeeds.
